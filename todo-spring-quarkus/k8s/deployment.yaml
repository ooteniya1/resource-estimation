apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: todo-spring-quarkus
  name: todo-spring-quarkus
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-spring-quarkus
  template:
    metadata:
      labels:
        app: todo-spring-quarkus
    spec:
      containers:
        - image: PIPELINE_REPLACE:latest
          imagePullPolicy: Always
          name: todo-spring-quarkus
          resources:
            limits:
              memory: "512Mi"
              cpu: "60m"  
            requests:
              memory: "128Mi"
              cpu: "30m"
          ports:
            - containerPort: 8080
              protocol: TCP

