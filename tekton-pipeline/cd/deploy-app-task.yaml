apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: deploy-app
spec:
  resources:
    inputs:
      - name: source
        type: git
  params:
    - name: manifest_dir
      description: The directory in source that contains yaml manifests
      default: "k8s"
    - name: contextDir
      description: The directory in source that contains yaml manifests
      default: .
  steps:
    - name: apply
      image: quay.io/openshift/origin-cli:latest
      workingDir: /workspace/source
      command: ["/bin/bash", "-c"]
      args:
        - |-
          echo Applying manifests in $(inputs.params.contextDir)/$(inputs.params.manifest_dir) directory
          oc apply -f $(inputs.params.contextDir)/$(inputs.params.manifest_dir)
          echo -----------------------------------
